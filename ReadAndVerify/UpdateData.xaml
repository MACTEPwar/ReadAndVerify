<Window x:Class="ReadAndVerify.UpdateData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReadAndVerify"
        mc:Ignorable="d"
        Title="UpdateData" Height="630" Width="983">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="271*"/>
            <RowDefinition Height="329*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="488*"/>
            <ColumnDefinition Width="487*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Name="dGrid" Margin="10,10,10,86" ItemsSource="{Binding Path=.}" Grid.ColumnSpan="2" Grid.RowSpan="2" AutoGeneratingColumn="dGrid_AutoGeneratingColumn" AutoGeneratedColumns="dGrid_AutoGeneratedColumns"/>
        <Button Margin="0,263,172,0" Grid.Row="1" HorizontalAlignment="Right" Width="257" Height="46" VerticalAlignment="Top">Отмена</Button>
        <Button Margin="172,263,0,0" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Width="257" Height="46" VerticalAlignment="Top">Сохранить</Button>
    </Grid>
</Window>
